# Конвертация HTML Copilot в Markdown для Obsidian

## Описание

Полный функционал для конвертации сохраненных HTML-файлов Microsoft Copilot в формат Markdown, оптимизированный для использования в Obsidian.

## Функции

### 1. `convertCopilotHtmlFileToMarkdown(htmlContent)`

Основная функция для конвертации полного HTML-файла Copilot в Markdown.

**Расположение:** `storage/export-md.js`

**Особенности:**
- ✅ Правильное разделение диалогов (Пользователь ↔ Copilot)
- ✅ Сохранение форматирования (заголовки, списки, код, ссылки)
- ✅ Обработка разделителей Copilot (`div` с классом `relative pb-6 w-full after:border-b`)
- ✅ Сохранение псевдокода со стрелками (→)
- ✅ Извлечение временных меток сообщений
- ✅ Удаление UI-элементов и служебных тегов

**Использование:**
```javascript
import { convertCopilotHtmlFileToMarkdown } from './storage/export-md.js';

const htmlContent = '<html>...</html>'; // HTML-код страницы Copilot
const markdown = convertCopilotHtmlFileToMarkdown(htmlContent);
console.log(markdown);
```

### 2. Улучшенная обработка разделителей

Функция `convertHtmlToMarkdown` теперь правильно обрабатывает разделители Copilot:
- Разделители конвертируются в горизонтальную линию `---`
- Сохраняется структура диалога

## Инструменты

### HTML-конвертер (автономный)

**Файл:** `copilot-html-to-md-standalone.html`

Автономный HTML-инструмент для конвертации сохраненных HTML-файлов.

**Использование:**
1. Откройте файл `copilot-html-to-md-standalone.html` в браузере
2. Перетащите HTML-файл Copilot в область загрузки или выберите файл
3. Нажмите "Конвертировать в Markdown"
4. Просмотрите результат в предпросмотре
5. Скачайте .md файл или скопируйте в буфер обмена

**Особенности:**
- ✅ Работает автономно (не требует сервера)
- ✅ Drag & Drop поддержка
- ✅ Предпросмотр результата
- ✅ Скачивание .md файла
- ✅ Копирование в буфер обмена

## Структура выходного Markdown

```markdown
# Диалог Copilot

**Дата конвертации:** [дата и время]

---

## 👤 Пользователь [временная метка]

[Содержимое сообщения пользователя]

## 🤖 Copilot [временная метка]

[Содержимое ответа Copilot]

[Форматирование сохраняется: заголовки, списки, код, ссылки, псевдокод]
```

## Поддерживаемые элементы

- ✅ Заголовки (H1-H6)
- ✅ Параграфы
- ✅ Списки (упорядоченные и неупорядоченные)
- ✅ Блоки кода с определением языка
- ✅ Inline код
- ✅ Ссылки
- ✅ Жирный и курсивный текст
- ✅ Цитаты (blockquote)
- ✅ Разделители Copilot
- ✅ Псевдокод со стрелками (→)
- ✅ Таблицы

## Примеры использования

### В расширении Chrome

```javascript
// В background.js или content.js
import { convertCopilotHtmlFileToMarkdown } from './storage/export-md.js';

// Получить HTML со страницы
const htmlContent = document.documentElement.outerHTML;

// Конвертировать
const markdown = convertCopilotHtmlFileToMarkdown(htmlContent);

// Сохранить или отправить
console.log(markdown);
```

### Через HTML-инструмент

1. Сохраните HTML-страницу Copilot (Ctrl+S в браузере)
2. Откройте `copilot-html-to-md-standalone.html`
3. Загрузите сохраненный HTML-файл
4. Получите готовый Markdown для Obsidian

## Технические детали

### Извлечение сообщений

Функция использует несколько стратегий для извлечения сообщений:

1. **Основная:** Поиск элементов с `role="article"`
2. **Альтернативная:** Поиск по `data-content="user-message"` и `data-content="ai-message"`

### Определение типа сообщения

- **Пользователь:** Наличие `data-content="user-message"` или соответствующих классов
- **Copilot:** Наличие `data-content="ai-message"` или соответствующих классов

### Очистка HTML

Удаляются следующие элементы:
- UI-элементы (кнопки, навигация, заголовки)
- Скрытые элементы (`aria-hidden="true"`, `.hidden`)
- Стили и скрипты
- SVG и изображения
- Реакции на сообщения

## Совместимость

- ✅ Obsidian (полная поддержка Markdown)
- ✅ Стандартный Markdown
- ✅ GitHub Flavored Markdown (GFM)

## Примечания

- Функция сохраняет порядок сообщений по их позиции в DOM
- Временные метки извлекаются из элементов `<time>` или атрибутов `datetime`
- Псевдокод со стрелками сохраняется как есть в тексте
- Разделители Copilot конвертируются в горизонтальные линии

## Следующие шаги

1. Протестировать конвертацию на реальном HTML-файле Copilot
2. Проверить корректность извлечения диалогов
3. Убедиться, что форматирование сохраняется правильно
4. Оптимизировать для Obsidian (добавить теги, ссылки и т.д.)
